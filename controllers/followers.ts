const getFollowers = () => {};

const createFollower = (id, user_id, follower_id) => {};

const deleteFollower = (id, user_id, follower_id) => {
  return false;
};

export { getFollowers, createFollower, deleteFollower };
